// Single source of truth for which backend this frontend talks to.
// Change ONLY this file when you deploy — login.html, register.html, and
// cineflow-ai.html all load this script and read window.API_BASE.
//
// Local development: leave as-is.
// After deploying the backend (Render/Railway/Cloud Run), replace with your
// real backend URL, e.g. "https://cineflow-backend.onrender.com" — no
// trailing slash.
window.API_BASE = "https://cineflow-ai-ex0g.onrender.com";
